[project]
name = "mysynthesizer-engine"
version = "0.1.0"
description = "Local MySynthesizer-compatible synthesis engine research prototype"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
